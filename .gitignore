# secrets — không bao giờ commit
secrets/
.env
*.json.key
service-account*.json
rclone.conf
cookies*.txt

# build / deps
node_modules/
dist/
__pycache__/
*.pyc
.venv/
venv/

# hệ điều hành
.DS_Store
.venv-dev/

# gói backup khoá — KHÔNG BAO GIỜ commit (đã lỡ 1 lần, commit 12dc68b)
*.tar.gz.enc
